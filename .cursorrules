# 示意/预留脚手架与 PLANNED 标记

## 与用户的回答风格（默认轻量化 · 强约束）

- **适用范围与优先级**：凡面向用户的**说明类**回复（概念、现状、对比、风险、排查结论、方案讨论、数据/指标含义等），**一律优先遵守本条**；其约束力**高于**「贴代码更易懂」「对照实现更严谨」等惯性，**不得以任何理由**在默认情况下回退成代码式讲解。
- **硬禁止（默认）**：除非用户在本轮对话中**明文**要求展示/张贴/用代码讲解（或等价表述），否则回复中**不得出现 Markdown 围栏代码块**（多行 fenced code block，即通常由成对代码围栏标记包裹的整块）。包括但不限于：仓库源码摘录、伪代码、「示例」脚本、用于论证的计算片段、较长 JSON/YAML/配置、多行 shell。论证「A 与 B 是否一致、差在哪」时，只写**分条文字结论**（各路径算什么、输入数据是否同源、误差可能来自哪里），**禁止**复制、缩写或翻写实现代码来证明。
- **写法**：结论用**短句分点**，一条一点；先答「对用户问题的直接结论」，再按需补前提或操作建议。需要指涉位置时，**少量**行内反引号标注**文件名或命令**即可；**不要**堆一长串函数名/符号列表代替正文结论。
- **与改代码任务的关系**：日常通过工具改仓库即可；**不要**在聊天里用大段源码复述刚改的内容，除非用户明确要求审阅代码或贴 diff。
- **允许的唯一默认例外**：收尾汇报中若必须写出**单行**可复制命令，可用**一行**行内反引号或纯文本写出（不要用多行围栏块）；仍优先用文字说明「跑了什么检查」。

## 禁止随意删除

- 本系统内所有**示意用、预留用**的脚手架（包括 UI 上的「PLANNED」/「planned」标签、对应占位结构、以及说明该功能尚未完成的注释等）**不得随意删除**。
- 在未接到明确指令时，**不要**批量「清理」或「简化」这些占位内容。

## 分模块实现时的规则

- 当用户指定「先做某一块 / 先实现某模块」时，**只允许在该模块范围内增加或修改实现代码**。
- **其他模块**的 PLANNED 标记、占位 UI、预留注释**必须原样保留**，不得一并删除或改为「已完成」状态。
- **当前模块**内的其他未实现子功能（如：做了数据层但没做画线工具），**其 PLANNED 坑位与原有 UI 描述也必须原样保留！** 绝对不可因实现当前模块的部分功能，就把该模块内尚未实现的其他 PLANNED 占位或 UI 抹除掉。做哪里，哪里才能去掉 PLANNED，没做的必须原样留存在 UI 上。

## 关联逻辑必须连通

- 当用户要求修改某个功能点时，必须先检查当前页面内其他功能点、其他页面功能点、共享数据层、共享状态、Worker/D1/API、快照导出、员工/LLM 分析输入等是否与该功能点存在真实逻辑关联。
- 只要存在真实逻辑关联，就必须把相关点一起纳入修改范围，保证入口、数据流、展示、状态、缓存、验证和错误处理前后一致；不得只修当前按钮/当前卡片/当前页面的一小段而留下断裂逻辑。
- 如果用户明确表示「先为当前功能点列一个修改 plan / 规划」，该 plan 也必须包含所有已识别的关联点、受影响文件、验证命令和部署/D1 操作，确保后续实现整体逻辑严丝合缝、十分严谨。
- 若某个关联点因风险、权限或范围原因暂时不能改，必须在 plan 或最终回复中明确标出原因、影响和后续处理方式。


## 何时可以删除本模块的 PLANNED

- 仅当**该模块所承诺的功能已完整实现并可用**时，才允许删除**这一处**与 PLANNED 相关的展示或标记。
- 未实现的模块其 PLANNED 必须**继续保留**，直至各自完成。

## Plan 类指令的确认义务

- 当用户下达 **plan / 规划 / 先规划** 等规划类意图的指令时，在动手改代码或写具体方案前，**必须先向用户确认**：
  - **当前要动的是哪一个模块**；
  - **该模块下本次规划所覆盖的「planned 范围」是什么**（例如：只做数据层、只做 UI 壳子、或端到端可交互等），避免在范围不清时误删或误改其他模块的预留内容。

- 若用户已在同一条消息里写清了模块名与范围，可简要复述确认后再执行，无需重复追问相同信息。

## Cloudflare Worker / D1 处理（仅按需执行）

- 凡是修改已部署在 Cloudflare 上的 Worker（如 `cloudflare/binance-klines-worker.js`）后，默认仅在本地完成校验，**除非用户明确要求部署**，否则不自动执行 `wrangler deploy`。
- 凡是修改 D1 schema、迁移 SQL 或需要调整远程 D1 表结构/表内容，**除非用户明确要求同步**，否则不自动执行远程 D1 迁移/写入命令。
- 若用户明确要求部署或同步但操作因登录、权限、网络或 Cloudflare 状态失败，须在回复中明确说明原因。

## Cloudflare Pages 自定义域名缓存

- 本项目 Pages 自定义域名是 `https://bitcoin.feiniwork.com/`。前端、静态资源、页面入口、样式或关键 JS 改动后，部署 Pages 前必须同步更新 `index.html` 中受影响资源的 `?v=` 版本号，避免自定义域名、浏览器或边缘缓存继续加载旧 `styles.css` / JS。
- 如果只是恢复、替换或修补 `styles.css`、`js/pages/*.js`、`js/app.js`、`js/config.js`、图表入口或其他由 `index.html` 引入的静态文件，也必须提升对应资源版本号；不要只部署文件本体。
- Pages 部署后必须核验 `https://bitcoin.feiniwork.com/` 返回的 `index.html` 已包含新 `?v=`，并至少检查受影响 hash 页面；若自定义域名和 `*.pages.dev` 内容不一致，优先排查 Pages production 指针、Cloudflare 缓存和浏览器缓存。
