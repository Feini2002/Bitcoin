# P10｜完成价值对照、跨页回归、迁移演练与开发收口

唯一入口：[EXECUTION_MASTER.md](../../EXECUTION_MASTER.md)。本文件只展开总任务的本阶段，不构成另一套独立计划。

## 1. 阶段产物

交付可复查的实现和测试记录，明确本地已完成、生产未验证、条件未触发及阻塞项；不以文档数量或页面可打开收口。

## 2. 开始条件

按实际完成能力汇总，不将任何核心任务默默移入可选。NEW-047已在P00建立记录，在这里检查新采用对象是否补齐。

本阶段顺序：NEW-043 → NEW-044 → NEW-048。实际硬依赖以每项前置为准；默认按总纲顺序执行，遇阻时只跳到依赖已满足的工作，不跳过受影响字段验证。每个任务完成后写日志，再继续下一就绪任务；不逐阶段要求用户重新批准本地scope。

## 3. 最小阅读范围

- [17_acceptance_and_experiments.md](../../00_current/17_acceptance_and_experiments.md)
- [15_migration_rollback.md](../../00_current/15_migration_rollback.md)
- [README.md](../../03_validation/README.md)

先按上列章节定位当前任务；无需重读第一批全部专题和67张候选卡。相关外部结论需要新核验时只补本次使用的来源，不把整个研究库日期更新成今天。

## 4. 本阶段执行链

1. 比较旧/新材料×模板/模型，缺少真实模型权限时只完成已能运行的组别，其余标not_run；不可伪造提升百分比。

2. 对每个已改数据页运行相关方法、读取、缓存、快照、导出和资源回归；已有全量build只有确认无生产副作用且环境满足才运行。

3. 在隔离副本演练兼容迁移和回退，确认未重写旧数据来源/报告；真实生产schema未知时保留阻塞，不能用空SQLite通过替代。

4. 依P11处理九项条件分支，每项均写触发/未触发/缺证据/待授权，不要求全部实现；代码正确性风险不得借条件分支推迟。

5. 输出实际变更文件、执行命令与日志、每项实现/验证/启用/效果状态、复用/替换理由、回退路径和下一就绪任务。准备发布提案可做，发布操作属于C45另行条件。

6. 更新既有仓库总入口的第二批进度链接，不改写第一批来源原件；没有明确指示不commit、push、开PR、部署或清理生产。

## 5. 阶段验收与跨页关系

1. 39项主线全部有可核查状态；未通过或仅模拟的项目不能写整体完成。

2. 66个原场景都有映射或明确本轮不适用原因；数量不是通过结果，失败不得删除。

3. 生产发布与本地实现结论分开；保留真实阻塞和可执行恢复位置，不再只建议重新制定计划。

相关原场景：CHECK-001、CHECK-002、CHECK-003、CHECK-004、CHECK-005、CHECK-006、CHECK-007、CHECK-008、CHECK-009、CHECK-010、CHECK-011、CHECK-012、CHECK-013、CHECK-014、CHECK-015、CHECK-016、CHECK-017、CHECK-018、CHECK-019、CHECK-020、CHECK-021、CHECK-022、CHECK-023、CHECK-024、CHECK-025、CHECK-026、CHECK-027、CHECK-028、CHECK-029、CHECK-030、CHECK-031、CHECK-032、CHECK-033、CHECK-034、CHECK-035、CHECK-036、CHECK-037、CHECK-038、CHECK-039、CHECK-040、CHECK-041、CHECK-042、CHECK-043、CHECK-044、CHECK-045、CHECK-046、CHECK-047、CHECK-048、CHECK-049、CHECK-050、CHECK-051、CHECK-052、CHECK-053、CHECK-054、CHECK-055、CHECK-056、CHECK-057、CHECK-058、CHECK-059、CHECK-060、CHECK-061、CHECK-062、CHECK-063、CHECK-064、CHECK-065、CHECK-066。P00是测试路由/基线，P10是全范围汇总；两者列全场景不表示开工前须完成全系统测试。

## 6. 阻塞与回退

只暂停失败能力和依赖，不把已正确页面整体回滚；若剩余均需外部权限，交付已完成代码、阻塞清单及精确恢复任务后停止本轮。

不得把“安全降级可用”写成原功能已实现；不得把缺生产权限写成代码不可继续开发。代码路径确实依赖当前缺失材料时暂停该依赖链，记录可继续的独立任务。

## 7. 逐任务执行规格

<a id="new-043"></a>
### NEW-043｜数据增量与模型增量分离评价

**责任与执行属性：**测试；默认主线，不需要再次选择切片。本包状态均为待执行，不是仓库测试通过记录。

**前置：**[NEW-003](P00_repository_binding.md#new-003)、[NEW-036](P03_market_template.md#new-036)

**已知定位线索：**`scripts/verify-yuqing-reports.cjs`、`scripts/verify-finance-datasets.cjs`。这些来自原审查基线；执行前确认当前路径、真实调用关系和当前修复状态，不编造函数/行号。

**修改范围：**定义旧/新材料×模板/模型对照；独立样本、抑制队列抽样、引用/噪音/遗漏/费用分母。

**本版收敛：**从P00/各阶段保留基线，这里完成对照汇总，不等最后才想样本。无真实样本则结果not_measured，不能用合成通过数宣传研究效果。

**新增与复用：**仅在既有布局无等价职责时拟新增；先由NEW-001绑定路径。优先现有等价实现；真实已完成的目标以代码和测试证明后复用，而不是为了打勾重写。

**关联核对：**本任务为公共约束或新增能力，不能据此声称仓库已经缺少相应实现。沿实际数据流核对页面、快照、报告输入、缓存、导出和历史；不相关消费者写明“不受影响及依据”，不是机械全改。

**迁移/失败/回退：**无增量的特征/源/角色移出默认，保留实验记录。 本地迁移先dry-run并验证旧reader；生产写入和删除不包含在默认本地任务中。

**验收步骤：**先保存当前行为或基线，按实际函数构造本任务输入；运行相关行为断言；沿实际消费者联调；对比方法/单位/时间身份；记录实跑命令、日志、差异和未跑原因。原验收要求：不能只用流畅度或低重复证明价值；模型组实际调用单独授权，未测不填提升。

**验收样本路由：**CHECK-037、CHECK-038、CHECK-040、CHECK-054、CHECK-055、CHECK-056、CHECK-058、CHECK-059、CHECK-061、CHECK-065。样本定义见[原66项规格](../../03_validation/acceptance_cases.json)。这是索引，不表示每项已经能直接作为API payload，需补真实函数必要字段。

**费用与外部边界：**仅本地代码/合成样本；安装、真实网络、费用、生产与部署另确认。默认可继续授权范围内的本地实现/离线验证；外部条件未满足只阻塞该调用或启用，不无声放宽限制。

**必须回答的未知：**真实获准样本、标注一致性、用户任务和费用目标。

**完成记录：**在[执行日志](../../EXECUTION_LOG.md)为NEW-043分别填写代码实现、离线/真实验证、运行启用和效果状态；若覆盖仅部分范围则partial，不能写完整任务done。


<a id="new-044"></a>
### NEW-044｜逐切片端到端与迁移演练

**责任与执行属性：**集成；默认主线，不需要再次选择切片。本包状态均为待执行，不是仓库测试通过记录。

**前置：**[NEW-003](P00_repository_binding.md#new-003)、[NEW-039](P03_market_template.md#new-039)、[NEW-040](P09_workspace_history.md#new-040)、[NEW-041](P09_workspace_history.md#new-041)、[NEW-043](P10_validation_handoff.md#new-043)

**已知定位线索：**`scripts/run-bounded.cjs`、`package.json`、`config/pages-assets.json`。这些来自原审查基线；执行前确认当前路径、真实调用关系和当前修复状态，不编造函数/行号。

**修改范围：**按选中slice执行对应语义、缓存、历史和资源测试；仅隔离副本演练，输出范围清单。

**本版收敛：**各阶段已有局部回归，这里合并执行已启用范围。某条件能力未启用可不适用，但核心未实现不能靠改分母全部变绿。

**新增与复用：**仅在既有布局无等价职责时拟新增；先由NEW-001绑定路径。优先现有等价实现；真实已完成的目标以代码和测试证明后复用，而不是为了打勾重写。

**关联核对：**本任务为公共约束或新增能力，不能据此声称仓库已经缺少相应实现。沿实际数据流核对页面、快照、报告输入、缓存、导出和历史；不相关消费者写明“不受影响及依据”，不是机械全改。

**迁移/失败/回退：**停止受影响切片，其他已验能力不回退；不drop生产表。 本地迁移先dry-run并验证旧reader；生产写入和删除不包含在默认本地任务中。

**验收步骤：**先保存当前行为或基线，按实际函数构造本任务输入；运行相关行为断言；沿实际消费者联调；对比方法/单位/时间身份；记录实跑命令、日志、差异和未跑原因。原验收要求：主图/足迹/强平/衍生品未启用项不算通过；有界测试、旧路径兼容、回退有效。

**验收样本路由：**CHECK-001、CHECK-002、CHECK-003、CHECK-004、CHECK-005、CHECK-006、CHECK-007、CHECK-008、CHECK-009、CHECK-010、CHECK-011、CHECK-012、CHECK-013、CHECK-014、CHECK-015、CHECK-016、CHECK-017、CHECK-018、CHECK-019、CHECK-020、CHECK-021、CHECK-022、CHECK-023、CHECK-024、CHECK-025、CHECK-026、CHECK-027、CHECK-028、CHECK-029、CHECK-030、CHECK-031、CHECK-032、CHECK-033、CHECK-034、CHECK-035、CHECK-036、CHECK-037、CHECK-038、CHECK-039、CHECK-040、CHECK-041、CHECK-042、CHECK-043、CHECK-044、CHECK-045、CHECK-046、CHECK-047、CHECK-048、CHECK-049、CHECK-050、CHECK-051、CHECK-052、CHECK-053、CHECK-054、CHECK-055、CHECK-056、CHECK-057、CHECK-058、CHECK-059、CHECK-060、CHECK-061、CHECK-062、CHECK-063、CHECK-064、CHECK-065、CHECK-066。样本定义见[原66项规格](../../03_validation/acceptance_cases.json)。这是索引，不表示每项已经能直接作为API payload，需补真实函数必要字段。

**费用与外部边界：**仅本地代码/合成样本；安装、真实网络、费用、生产与部署另确认。默认可继续授权范围内的本地实现/离线验证；外部条件未满足只阻塞该调用或启用，不无声放宽限制。

**必须回答的未知：**测试脚本网络副作用、实际部署边界。

**完成记录：**在[执行日志](../../EXECUTION_LOG.md)为NEW-044分别填写代码实现、离线/真实验证、运行启用和效果状态；若覆盖仅部分范围则partial，不能写完整任务done。


<a id="new-048"></a>
### NEW-048｜开发接手与范围闭合

**责任与执行属性：**集成；默认主线，不需要再次选择切片。本包状态均为待执行，不是仓库测试通过记录。

**前置：**[NEW-043](P10_validation_handoff.md#new-043)、[NEW-044](P10_validation_handoff.md#new-044)、[NEW-047](P00_repository_binding.md#new-047)

**已知定位线索：**`README.md`、`docs/research/bitcoin-upgrade/README.md`。这些来自原审查基线；执行前确认当前路径、真实调用关系和当前修复状态，不编造函数/行号。

**修改范围：**输出已实现/部分/未启用/未验证矩阵、方法与接口实际位置、费用、失败和后续触发，维护单一入口。

**本版收敛：**收口报告的核心是准确状态和恢复位置；不要求把条件项全部做完，也不允许把受阻主线称为完成。

**新增与复用：**仅在既有布局无等价职责时拟新增；先由NEW-001绑定路径。优先现有等价实现；真实已完成的目标以代码和测试证明后复用，而不是为了打勾重写。

**关联核对：**本任务为公共约束或新增能力，不能据此声称仓库已经缺少相应实现。沿实际数据流核对页面、快照、报告输入、缓存、导出和历史；不相关消费者写明“不受影响及依据”，不是机械全改。

**迁移/失败/回退：**未过项留状态，不为全部绿色启用无用planned模块。 本地迁移先dry-run并验证旧reader；生产写入和删除不包含在默认本地任务中。

**验收步骤：**先保存当前行为或基线，按实际函数构造本任务输入；运行相关行为断言；沿实际消费者联调；对比方法/单位/时间身份；记录实跑命令、日志、差异和未跑原因。原验收要求：不能把历史研究卡、合成样本数和本包任务数当实现数；当前用户能复查结果。

**验收样本路由：**本阶段的工作区/文档绑定与收口清单；不得用文档存在代替代码或效果证明。。样本定义见[原66项规格](../../03_validation/acceptance_cases.json)。这是索引，不表示每项已经能直接作为API payload，需补真实函数必要字段。

**费用与外部边界：**仅本地代码/合成样本；安装、真实网络、费用、生产与部署另确认。默认可继续授权范围内的本地实现/离线验证；外部条件未满足只阻塞该调用或启用，不无声放宽限制。

**必须回答的未知：**实际启用scope、生产观察是否完成、后续维护owner。

**完成记录：**在[执行日志](../../EXECUTION_LOG.md)为NEW-048分别填写代码实现、离线/真实验证、运行启用和效果状态；若覆盖仅部分范围则partial，不能写完整任务done。


## 8. 阶段检查点与下一步

主线本地收口或诚实的部分收口。若明确获授权且C45条件满足，再按C45发布；否则不部署。

执行日志必须指出下一条就绪任务及确切文件/未完成步骤。阶段检查点是开发反馈，不是产品运行审批流程。若本轮交互结束，记录恢复位置；不能声称无需后续交互仍会自动在后台开发。
