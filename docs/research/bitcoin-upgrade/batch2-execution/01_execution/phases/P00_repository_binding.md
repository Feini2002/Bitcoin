# P00｜绑定当前仓库与已归档资料，建立测试和权限基线

唯一入口：[EXECUTION_MASTER.md](../../EXECUTION_MASTER.md)。本文件只展开总任务的本阶段，不构成另一套独立计划。

## 1. 阶段产物

第一批资料原位保留；第二批只增加一个执行入口。确认真实代码位置、已有修复、共享文件负责人、命令副作用与离线测试入口，然后继续开发，不止交一份盘点报告。

## 2. 开始条件

先读根 EXECUTION_MASTER.md。当前代码、已有归档和用户本次授权尚未绑定时，只做只读核验；不得把固定提交的审查结论无条件写成当前缺陷。

本阶段顺序：NEW-001 → NEW-002 → NEW-003 → NEW-047。实际硬依赖以每项前置为准；默认按总纲顺序执行，遇阻时只跳到依赖已满足的工作，不跳过受影响字段验证。每个任务完成后写日志，再继续下一就绪任务；不逐阶段要求用户重新批准本地scope。

## 3. 最小阅读范围

- [01_repository_baseline.md](../../00_current/01_repository_baseline.md)
- [02_findings_and_counterexamples.md](../../00_current/02_findings_and_counterexamples.md)
- [15_migration_rollback.md](../../00_current/15_migration_rollback.md)
- [19_sources_and_review_boundary.md](../../00_current/19_sources_and_review_boundary.md)

先按上列章节定位当前任务；无需重读第一批全部专题和67张候选卡。相关外部结论需要新核验时只补本次使用的来源，不把整个研究库日期更新成今天。

## 4. 本阶段执行链

1. 定位当前仓库根、分支、HEAD、相关未提交/未跟踪变更；只记录必要摘要，不复制密钥，不要求清空工作区。逐项将F01—F30映射为仍成立、已修复、需实验、证据不足或不适用。

2. 找到第一批实际归档目录和既有README/QUICK_ROUTER/主题索引。优先增加一个“第二批执行”链接和四页/情报/分析主题指针，不重新解压第一批、不覆盖原文件、不再部署文档搜索系统。

3. 核对 package.json 和实际测试脚本的副作用。只运行已授权的本地检查；线上探活、导入、清理和部署命令不因在scripts中出现就运行。保存改动前相关函数输出与当前测试结果。

4. 按本次将使用的来源登记用途和权限；只核相关官方资料、字段与服务条件。不能因尚未审完67个候选而阻塞全部离线开发。已存在的来源/方法登记优先复用。

5. 初始化根 EXECUTION_LOG.md 的工作区、权限和状态；给共享finance、行情Worker、snapshot、yuqing、导航/发布清单指定一个合并责任方。完成后直接进入P01。

## 5. 阶段验收与跨页关系

1. 原第一批文件未被覆盖、重排或重复标成待执行；根执行入口唯一。

2. 每个即将修改的职责都有真实路径或明确待查项；历史PASS未冒充本轮测试。

3. 已修复项目有当前实现和验证证据，不再次重写；外部限制只阻塞对应用途。

相关原场景：CHECK-001、CHECK-002、CHECK-003、CHECK-004、CHECK-005、CHECK-006、CHECK-007、CHECK-008、CHECK-009、CHECK-010、CHECK-011、CHECK-012、CHECK-013、CHECK-014、CHECK-015、CHECK-016、CHECK-017、CHECK-018、CHECK-019、CHECK-020、CHECK-021、CHECK-022、CHECK-023、CHECK-024、CHECK-025、CHECK-026、CHECK-027、CHECK-028、CHECK-029、CHECK-030、CHECK-031、CHECK-032、CHECK-033、CHECK-034、CHECK-035、CHECK-036、CHECK-037、CHECK-038、CHECK-039、CHECK-040、CHECK-041、CHECK-042、CHECK-043、CHECK-044、CHECK-045、CHECK-046、CHECK-047、CHECK-048、CHECK-049、CHECK-050、CHECK-051、CHECK-052、CHECK-053、CHECK-054、CHECK-055、CHECK-056、CHECK-057、CHECK-058、CHECK-059、CHECK-060、CHECK-061、CHECK-062、CHECK-063、CHECK-064、CHECK-065、CHECK-066。P00是测试路由/基线，P10是全范围汇总；两者列全场景不表示开工前须完成全系统测试。

## 6. 阻塞与回退

第一批路径暂未找到时，使用本包90_reference作为只读备用并记录reference_binding_pending，继续不依赖归档定位的源码工作。发现共享文件并发变化，只暂停该文件补丁。

不得把“安全降级可用”写成原功能已实现；不得把缺生产权限写成代码不可继续开发。代码路径确实依赖当前缺失材料时暂停该依赖链，记录可继续的独立任务。

## 7. 逐任务执行规格

<a id="new-001"></a>
### NEW-001｜锁定本次工作区和能力范围

**责任与执行属性：**集成；默认主线，不需要再次选择切片。本包状态均为待执行，不是仓库测试通过记录。

**前置：**无；以本次任务的明确权限和实际工作区为起点。

**已知定位线索：**`README.md`、`package.json`、`config/pages-assets.json`、`docs/research/bitcoin-upgrade/README.md`。这些来自原审查基线；执行前确认当前路径、真实调用关系和当前修复状态，不编造函数/行号。

**修改范围：**比对a6ef6c8与实际本机差异；只绑定本次受影响模块、schema、现有测试和治理owner，列已修复/仍成立/未知。

**本版收敛：**本任务包含把第二批挂到现有第一批归档入口，但不重整第一批正文。绑定后继续执行固定主线，不再把“请先选择S1—S6”作为结束条件。

**新增与复用：**仅在既有布局无等价职责时拟新增；先由NEW-001绑定路径。优先现有等价实现；真实已完成的目标以代码和测试证明后复用，而不是为了打勾重写。

**关联核对：**本任务为公共约束或新增能力，不能据此声称仓库已经缺少相应实现。沿实际数据流核对页面、快照、报告输入、缓存、导出和历史；不相关消费者写明“不受影响及依据”，不是机械全改。

**迁移/失败/回退：**冲突文件暂停补丁，按新基线重做三方对照，不覆盖未知变化。 本地迁移先dry-run并验证旧reader；生产写入和删除不包含在默认本地任务中。

**验收步骤：**先保存当前行为或基线，按实际函数构造本任务输入；运行相关行为断言；沿实际消费者联调；对比方法/单位/时间身份；记录实跑命令、日志、差异和未跑原因。原验收要求：形成可复核路径表、文件摘要与未提交变更保护清单；没有reset或全仓重新治理。

**验收样本路由：**本阶段的工作区/文档绑定与收口清单；不得用文档存在代替代码或效果证明。。样本定义见[原66项规格](../../03_validation/acceptance_cases.json)。这是索引，不表示每项已经能直接作为API payload，需补真实函数必要字段。

**费用与外部边界：**仅本地代码/合成样本；安装、真实网络、费用、生产与部署另确认。默认可继续授权范围内的本地实现/离线验证；外部条件未满足只阻塞该调用或启用，不无声放宽限制。

**必须回答的未知：**实际分支、未跟踪文件、并发治理与远程版本均需确认。

**完成记录：**在[执行日志](../../EXECUTION_LOG.md)为NEW-001分别填写代码实现、离线/真实验证、运行启用和效果状态；若覆盖仅部分范围则partial，不能写完整任务done。


<a id="new-002"></a>
### NEW-002｜登记启用来源的用途与运行条件

**责任与执行属性：**来源；默认主线，不需要再次选择切片。本包状态均为待执行，不是仓库测试通过记录。

**前置：**[NEW-001](P00_repository_binding.md#new-001)

**已知定位线索：**`cloudflare/finance/registry.mjs`、`cloudflare/finance/gateway.mjs`、`cloudflare/yuqing/yuqing-worker.js`。这些来自原审查基线；执行前确认当前路径、真实调用关系和当前修复状态，不编造函数/行号。

**修改范围：**只核当前切片需要的API、访问凭据、保存/模型/导出用途、地区与费用，不审核全部67候选后才开工。

**本版收敛：**形成来源用途记录不等于批准所有用途。unknown/denied是合法登记值；它们阻塞实际取用/外发，而不阻塞使用合成样本完成适配和拒绝路径。

**新增与复用：**仅在既有布局无等价职责时拟新增；先由NEW-001绑定路径。优先现有等价实现；真实已完成的目标以代码和测试证明后复用，而不是为了打勾重写。

**关联核对：**本任务为公共约束或新增能力，不能据此声称仓库已经缺少相应实现。沿实际数据流核对页面、快照、报告输入、缓存、导出和历史；不相关消费者写明“不受影响及依据”，不是机械全改。

**迁移/失败/回退：**隔离受限用途，保留允许的模板与市场读取。 本地迁移先dry-run并验证旧reader；生产写入和删除不包含在默认本地任务中。

**验收步骤：**先保存当前行为或基线，按实际函数构造本任务输入；运行相关行为断言；沿实际消费者联调；对比方法/单位/时间身份；记录实跑命令、日志、差异和未跑原因。原验收要求：allowed/denied/unknown三种用途样本在读取、外发与导出分别正确；未知不自动允许。

**验收样本路由：**CHECK-066。样本定义见[原66项规格](../../03_validation/acceptance_cases.json)。这是索引，不表示每项已经能直接作为API payload，需补真实函数必要字段。

**费用与外部边界：**官方资料只读；合同、账号、真实API及付费另确认。默认可继续授权范围内的本地实现/离线验证；外部条件未满足只阻塞该调用或启用，不无声放宽限制。

**必须回答的未知：**真实合同、provider计划、展示范围、外层认证。

**完成记录：**在[执行日志](../../EXECUTION_LOG.md)为NEW-002分别填写代码实现、离线/真实验证、运行启用和效果状态；若覆盖仅部分范围则partial，不能写完整任务done。


<a id="new-003"></a>
### NEW-003｜把审查反例接到现有测试入口

**责任与执行属性：**测试；默认主线，不需要再次选择切片。本包状态均为待执行，不是仓库测试通过记录。

**前置：**[NEW-001](P00_repository_binding.md#new-001)

**已知定位线索：**`scripts/verify-indicator-math.cjs`、`scripts/verify-footprint-aggregation.cjs`、`scripts/verify-liquidation-radar.cjs`、`scripts/verify-finance-datasets.cjs`、`package.json`。这些来自原审查基线；执行前确认当前路径、真实调用关系和当前修复状态，不编造函数/行号。

**修改范围：**把本包相关合成场景映射现有函数；保留已正确方向测试；数值测试与源码字符串测试分开。

**本版收敛：**按阶段接入相应场景而非第一天实现66个全量业务测试；本任务负责框架与相关基线，NEW-044负责最终映射和全范围回归。历史测试记录不得重标为今天已跑。

**新增与复用：**仅在既有布局无等价职责时拟新增；先由NEW-001绑定路径。优先现有等价实现；真实已完成的目标以代码和测试证明后复用，而不是为了打勾重写。

**关联核对：**本任务为公共约束或新增能力，不能据此声称仓库已经缺少相应实现。沿实际数据流核对页面、快照、报告输入、缓存、导出和历史；不相关消费者写明“不受影响及依据”，不是机械全改。

**迁移/失败/回退：**新测试环境问题隔离而非删除反例，记录未运行范围。 本地迁移先dry-run并验证旧reader；生产写入和删除不包含在默认本地任务中。

**验收步骤：**先保存当前行为或基线，按实际函数构造本任务输入；运行相关行为断言；沿实际消费者联调；对比方法/单位/时间身份；记录实跑命令、日志、差异和未跑原因。原验收要求：原行为差异可定位到字段/方法；每个新assert有expected依据，不靠删失败样本变绿。

**验收样本路由：**CHECK-001、CHECK-002、CHECK-003、CHECK-004、CHECK-005、CHECK-006、CHECK-007、CHECK-008、CHECK-009、CHECK-010、CHECK-011、CHECK-012、CHECK-013、CHECK-014、CHECK-015、CHECK-016、CHECK-017、CHECK-018、CHECK-019、CHECK-020、CHECK-021、CHECK-022、CHECK-023、CHECK-024、CHECK-025、CHECK-026、CHECK-027、CHECK-028、CHECK-029、CHECK-030、CHECK-031、CHECK-032、CHECK-033、CHECK-034、CHECK-035、CHECK-036、CHECK-037、CHECK-038、CHECK-039、CHECK-040、CHECK-041、CHECK-042、CHECK-043、CHECK-044、CHECK-045、CHECK-046、CHECK-047、CHECK-048、CHECK-049、CHECK-050、CHECK-051、CHECK-052、CHECK-053、CHECK-054、CHECK-055、CHECK-056、CHECK-057、CHECK-058、CHECK-059、CHECK-060、CHECK-061、CHECK-062、CHECK-063、CHECK-064、CHECK-065、CHECK-066。样本定义见[原66项规格](../../03_validation/acceptance_cases.json)。这是索引，不表示每项已经能直接作为API payload，需补真实函数必要字段。

**费用与外部边界：**仅本地代码/合成样本；安装、真实网络、费用、生产与部署另确认。默认可继续授权范围内的本地实现/离线验证；外部条件未满足只阻塞该调用或启用，不无声放宽限制。

**必须回答的未知：**哪些脚本联网、Node与实际模块加载方式。

**完成记录：**在[执行日志](../../EXECUTION_LOG.md)为NEW-003分别填写代码实现、离线/真实验证、运行启用和效果状态；若覆盖仅部分范围则partial，不能写完整任务done。


<a id="new-047"></a>
### NEW-047｜对手方资料和公共源复核登记

**责任与执行属性：**研究；默认主线，不需要再次选择切片。本包状态均为待执行，不是仓库测试通过记录。

**前置：**[NEW-001](P00_repository_binding.md#new-001)、[NEW-002](P00_repository_binding.md#new-002)

**已知定位线索：**`docs/research/bitcoin-upgrade/README.md`。这些来自原审查基线；执行前确认当前路径、真实调用关系和当前修复状态，不编造函数/行号。

**修改范围：**当前来源与历史参考分开；采用候选只核对应版本、许可、API与方法，不把旧150条重标今天verified。

**本版收敛：**仅复核本次实际采用的接口、许可、方法和版本；记录查阅日期与支持范围，不能把历史150条来源日期全部改成今天。

**新增与复用：**仅在既有布局无等价职责时拟新增；先由NEW-001绑定路径。优先现有等价实现；真实已完成的目标以代码和测试证明后复用，而不是为了打勾重写。

**关联核对：**本任务为公共约束或新增能力，不能据此声称仓库已经缺少相应实现。沿实际数据流核对页面、快照、报告输入、缓存、导出和历史；不相关消费者写明“不受影响及依据”，不是机械全改。

**迁移/失败/回退：**弱证据候选降为待试验/思想参考，不删除历史。 本地迁移先dry-run并验证旧reader；生产写入和删除不包含在默认本地任务中。

**验收步骤：**先保存当前行为或基线，按实际函数构造本任务输入；运行相关行为断言；沿实际消费者联调；对比方法/单位/时间身份；记录实跑命令、日志、差异和未跑原因。原验收要求：每项采用结论有具体接口/方法与竞争替代；失败来源明确未知。

**验收样本路由：**本阶段的工作区/文档绑定与收口清单；不得用文档存在代替代码或效果证明。。样本定义见[原66项规格](../../03_validation/acceptance_cases.json)。这是索引，不表示每项已经能直接作为API payload，需补真实函数必要字段。

**费用与外部边界：**仅本地代码/合成样本；安装、真实网络、费用、生产与部署另确认。默认可继续授权范围内的本地实现/离线验证；外部条件未满足只阻塞该调用或启用，不无声放宽限制。

**必须回答的未知：**当前供应商合同/工具版本/停维护状态。

**完成记录：**在[执行日志](../../EXECUTION_LOG.md)为NEW-047分别填写代码实现、离线/真实验证、运行启用和效果状态；若覆盖仅部分范围则partial，不能写完整任务done。


## 8. 阶段检查点与下一步

记录工作区基线、可执行命令清单、第一批绑定位置与初始失败项；下一主线P01。

执行日志必须指出下一条就绪任务及确切文件/未完成步骤。阶段检查点是开发反馈，不是产品运行审批流程。若本轮交互结束，记录恢复位置；不能声称无需后续交互仍会自动在后台开发。
