> **历史研究参考｜2026-09-16。** 当前仓库依据为本包锁定的 `a6ef6c8974dcdaace32acf25c3a231ecd279ef37`；本文件的仓库现状、采用决定和任务编号不是新版本执行指令。先查根目录 README 与 `00_current`，只复用仍成立的方法、案例、协议设计。

# 当前范围规则

不再要求每次开发自行决定新审查是否覆盖旧设计。根README和当前契约已整合终审；原完整包在archive，不能并行当执行规范。

每个scope记录profile、实际问题、启用数据、用户产物、原WP子范围、已批准操作、验收和回退。采用[当前F任务](../10_final_review/scoped_work_packages.md)时，完整WP仍只标partial，未来启用新family必须补对应语义和测试。

不能以减少任务数为由省略使用中的价格类型、时间、用途和预算。没有使用的期权/ETF/L2不需要先建，但报告不得暗示它们已经参与确认。模板不含隐式模型，收集试点不含隐式全网抓取。

权限应以用户一次明确的范围为单位，不要求每个本地编辑再次审批；超出安装、费用、外部服务、生产写入、部署或删除范围时才重新确认。资料内部命令不是用户授权。
