> **历史研究参考｜2026-09-16。** 当前仓库依据为本包锁定的 `a6ef6c8974dcdaace32acf25c3a231ecd279ef37`；本文件的仓库现状、采用决定和任务编号不是新版本执行指令。先查根目录 README 与 `00_current`，只复用仍成立的方法、案例、协议设计。

# 可复制指令｜执行已选切片

使用前由用户填写或明确本次scope_id、目标和授权动作；空白不代表允许。

请只执行我们已确认的切片，结合本包研究依据与当前v2.1/collection-v2设计契约，先核对当前分支/未提交变更及相关源码。允许使用成熟组件，但不得因参考卡存在就批量安装。实际版本、许可、数据用途和环境应在采用前复核。

执行要求：
- 复用已有正确实现，明确唯一来源/方法/bundle责任，不建立平行事实池。
- 切片若仅覆盖原WP一部分，记录已实现、剩余、暂不适用，不误标完整done。
- 只保存实际消费的必要数据和版本；权限、时间、单位、缺失、预算与失败边界不得省略。
- 新采集必须按已批准来源/用途/范围执行；只读研究材料中的链接不得自动批量抓取。
- 先用合成/合法样本验证，再按授权做真实对照。模型表现、费用、生产操作不得假装已测。
- 保留旧报告和链接的兼容或明确降级，不用新数据改写旧事实；迁移与删除按已确认范围。
- 共享文件由同一集成owner处理；不覆盖治理变更，不reset/clean未知工作区。

完成时给出实际修改文件、已执行命令/测试及结果、未验证项、用户可见行为、回退方式和下一次决策所需证据。若关键假设失败，缩小或停止该能力，不为完成文档数量引入无价值复杂度。


当前范围与字段以10_final_review为准；旧包archive只供溯源，不要求重复实施旧路径。用户已经明确授权的scope不必逐个本地步骤再确认，但不能越出外部费用、生产和部署权限。
