> **历史研究参考｜2026-09-16。** 当前仓库依据为本包锁定的 `a6ef6c8974dcdaace32acf25c3a231ecd279ef37`；本文件的仓库现状、采用决定和任务编号不是新版本执行指令。先查根目录 README 与 `00_current`，只复用仍成立的方法、案例、协议设计。

# TOOL010｜SearXNG

查询日期：2026-09-16。已核验事实仅限下列直接材料；实验未执行。

## 身份与直接证据

类型：元搜索服务。

已核验：提供统一查询接口，将多个搜索引擎结果组织成检索输出。它是发现层，不是多个独立事实来源。

[EXT025｜SearXNG 官方仓库](https://github.com/searxng/searxng) [EXT026｜SearXNG Search API](https://docs.searxng.org/dev/search_api.html)

## 可以吸收的具体成果

可独立部署作为研究查询入口，保存查询式、时间范围、被检索引擎与候选URL；把查询结果交给允许的原始来源读取，而不是直接把snippet变成事实。

## 不适用与负面证据

实例可能禁用JSON、遭上游限流或只有部分引擎可用。十个引擎返回同一通讯稿并不构成十份证据；搜索排名不能代替金融重要性。

## 代码、数据与服务权利

仓库标识AGPL-3.0；具体锁定版本/依赖许可待复核。引擎条款和原始内容使用权独立，公共实例访问不构成服务承诺。

## 运行与成本边界

独立服务及网络配置；成本由查询量、缓存与上游限制决定。不能承诺自托管即可免费无限搜索。

## 维护证据及其限度

当前API文档可读且有版本化入口；本轮未测各引擎或公共实例，因此搜索覆盖率未知。

## 竞争替代与选型条件

许可明确的商业搜索API或直接官方站点检索都应对照。只搜索已登记来源时，限定站内/目录发现可能更可靠。

## 最低成本核验实验

给定研究问题和已知原始发布，测来源发现、重复、漏检与可复读率；记录每个引擎失败。不允许将未读取的摘要写成“已核验”。

## 两轴判断

**一般适用性：**适合需要掌控发现层与检索记录的系统。

**当前仓库适用性：**与现有Google Grounding是不同路线；对外使用权和成本确认前只做设计候选。
