> **历史研究参考｜2026-09-16。** 当前仓库依据为本包锁定的 `a6ef6c8974dcdaace32acf25c3a231ecd279ef37`；本文件的仓库现状、采用决定和任务编号不是新版本执行指令。先查根目录 README 与 `00_current`，只复用仍成立的方法、案例、协议设计。

# TOOL007｜Scrapy

查询日期：2026-09-16。已核验事实仅限下列直接材料；实验未执行。

## 身份与直接证据

类型：可扩展采集框架。

已核验：JOBDIR机制用于持久爬取任务；暂停恢复涉及请求序列化及状态等约束。BSD-3-Clause许可。

[EXT017｜Scrapy 官方仓库](https://github.com/scrapy/scrapy) [EXT018｜Scrapy 持久任务](https://docs.scrapy.org/en/latest/topics/jobs.html) [EXT019｜Scrapy 许可证](https://raw.githubusercontent.com/scrapy/scrapy/master/LICENSE)

## 可以吸收的具体成果

批量公开资料发现、分页与历史回补、按域并发、可恢复采集和管道分层适合复用。应让框架承担请求/队列，让专用适配器承担金融时间和字段语义。

## 不适用与负面证据

断点恢复不保证上游历史窗口仍在，也不保证Cookie有效或所有请求可序列化。没有站点授权时，成熟爬虫不是许可替代；也不应以高并发冲击对方服务。

## 代码、数据与服务权利

代码BSD-3-Clause；源网站/API规则和第三方下载中间件另审。采集到的新闻不变成项目自己版权。

## 运行与成本边界

Python进程和持久任务目录；长时间回补需磁盘、调度与进程监控。对于只有几条RSS，框架可能比任务本身更复杂。

## 维护证据及其限度

官方当前文档和源码有明确恢复机制；本轮没有跑Spider、升级或故障恢复。每个站点解析器仍需维护。

## 竞争替代与选型条件

简单HTTP/官方API任务与Scrapy对照；大量动态页面再比较浏览器工具，不让它同时承担所有浏览器自动化。

## 最低成本核验实验

使用获准静态样本和模拟分页，执行计划上测试中断、续跑、重复、Cookie失效与游标窗口过期。若恢复后不能解释缺口，禁止声称完整历史。

## 两轴判断

**一般适用性：**适合采集复杂度已经超过小脚本的服务，与研究架构无关。

**当前仓库适用性：**当前不必替换已有交易所采集；新增新闻历史批处理时可以独立采用。
