> **历史研究参考｜2026-09-16。** 当前仓库依据为本包锁定的 `a6ef6c8974dcdaace32acf25c3a231ecd279ef37`；本文件的仓库现状、采用决定和任务编号不是新版本执行指令。先查根目录 README 与 `00_current`，只复用仍成立的方法、案例、协议设计。

# TOOL005｜Mozilla Readability

查询日期：2026-09-16。已核验事实仅限下列直接材料；实验未执行。

## 身份与直接证据

类型：JavaScript正文提取库。

已核验：README描述浏览器/DOM正文提取，并明确说明其不负责HTML消毒；在服务器DOM环境下应保持脚本等危险行为关闭。

[EXT013｜Mozilla Readability 原始说明](https://raw.githubusercontent.com/mozilla/readability/main/README.md)

## 可以吸收的具体成果

直接依赖用于允许抓取的静态HTML正文抽取，保留原URL和源文档版本；与现有JS采集环境兼容性值得先验证。抽取结果保留段落映射，以便引用定位而不只生成一段无法回溯的纯文本。

## 不适用与负面证据

不能保证表格、图注、脚注或发布日期准确；不是通用浏览器，也不是事实核查器。删除了否定词、单位或表头的“干净正文”比原页面更危险。

## 代码、数据与服务权利

README说明Apache-2.0。源内容许可独立；渲染抽取HTML前仍需安全净化与内容安全策略，不能把库许可当输出安全保证。

## 运行与成本边界

JS/DOM；服务端通常需要兼容DOM实现，实际依赖与Workers能力需查。没有模型费，但正文解析和大页面内存仍有成本。

## 维护证据及其限度

官方说明与安全边界明确；本轮未跑站点样本或锁定发布回归，不以低发布频率判定停止维护。

## 竞争替代与选型条件

Trafilatura在Python研究环境可作为竞争提取器；有结构化RSS/JSON字段时无需再抓正文。优先比较信息保真而不是字符提取量。

## 最低成本核验实验

用包含否定、日期、表格、脚注与付费截断的许可样本做对照。记录关键句保留率、错误正文进入率与失败可识别性；输出中不得执行外部脚本。

## 两轴判断

**一般适用性：**适合较轻量正文抽取，复用价值明确但职责有限。

**当前仓库适用性：**与当前JS适配相容的实验起点；不能据此认定必须使用它替换已有解析。
