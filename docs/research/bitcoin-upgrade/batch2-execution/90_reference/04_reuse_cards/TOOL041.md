> **历史研究参考｜2026-09-16。** 当前仓库依据为本包锁定的 `a6ef6c8974dcdaace32acf25c3a231ecd279ef37`；本文件的仓库现状、采用决定和任务编号不是新版本执行指令。先查根目录 README 与 `00_current`，只复用仍成立的方法、案例、协议设计。

# TOOL041｜dlt

查询日期：2026-09-16。已核验事实仅限下列直接材料；实验未执行。

## 身份与直接证据

类型：增量数据加载工具。

官方游标文档说明增量状态、边界与去重；游标回答从哪里继续读取，不证明上游过去的记录不会修订。

[EXT083｜dlt 官方仓库](https://github.com/dlt-hub/dlt) [EXT084｜dlt 增量游标文档](https://dlthub.com/docs/general-usage/incremental/cursor)

## 可以吸收的具体成果

适合多个结构化API到存储的重复加载；把游标、主键、加载批次和目的端状态独立保存。对可变历史另设回看窗口和完整校验。

## 不适用与负面证据

只按最大时间前进会漏掉迟到记录、日期不变的更正或分页期间插入。自动schema演进也可能让金融单位变化悄悄进入同名列。

## 代码、数据与服务权利

本轮未独立逐项审计所有连接器与发行许可；主库和目的端服务、原始API条款分别确认后才能采用。

## 运行与成本边界

Python加载环境，目标数据库/对象存储按需求；增量状态和失败批次要有恢复策略。

## 维护证据及其限度

当前文档/仓库可核，实际连接器维护与数据源兼容未知。

## 竞争替代与选型条件

少数稳定API用原生ETL更直接；大量重复分页、状态和目的端适配时dlt有价值。

## 最低成本核验实验

合成分页插入、边界重复、迟到和旧记录更正，重跑后验证不丢不重复且修订有版本。不是成功加载一次就通过。

## 两轴判断

**一般适用性：**有用的数据工程专用车轮。

**当前仓库适用性：**适合未来结构化来源增多，先与已有同步逻辑对照。
