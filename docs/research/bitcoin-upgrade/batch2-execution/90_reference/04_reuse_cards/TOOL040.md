> **历史研究参考｜2026-09-16。** 当前仓库依据为本包锁定的 `a6ef6c8974dcdaace32acf25c3a231ecd279ef37`；本文件的仓库现状、采用决定和任务编号不是新版本执行指令。先查根目录 README 与 `00_current`，只复用仍成立的方法、案例、协议设计。

# TOOL040｜Polars

查询日期：2026-09-16。已核验事实仅限下列直接材料；实验未执行。

## 身份与直接证据

类型：列式数据变换库。

官方项目提供列式DataFrame与查询能力，适合大批量变换；它不是行情服务、调度器或模型正确性检查器。

[EXT106｜Polars 当前仓库](https://github.com/pola-rs/polars) [EXT107｜Polars 许可证](https://raw.githubusercontent.com/pola-rs/polars/main/LICENSE)

## 可以吸收的具体成果

复杂分组、窗口和join可由一次离线计算生成研究结果；显式指定时间列、排序和类型，输出method_version和输入清单。

## 不适用与负面证据

将全部SQL改写成另一份DataFrame逻辑可能制造平行真值。浮点、空值、时区与窗口边界需黄金样本，不能只比较运行速度。

## 代码、数据与服务权利

MIT文本已核；外部连接器、扩展和数据另外审。

## 运行与成本边界

Python/Rust等绑定按目标发行；多线程、懒查询和内存行为需代表性数据测量，不使用营销benchmark预测本项目。

## 维护证据及其限度

当前仓库与许可可读；具体发行回归、插件和平台支持未全面审计。

## 竞争替代与选型条件

DuckDB SQL对研究查询更透明；Polars适合SQL难表达的变换。可以互补，但同一权威指标只维护一个实现或明确交叉校验版本。

## 最低成本核验实验

用空值、重复时间、乱序、时区及窗口边界样本对照现有计算，确认排序和结果；性能收益不能掩盖语义改变。

## 两轴判断

**一般适用性：**成熟数据变换候选。

**当前仓库适用性：**只在具体变换节省复杂度时采用，不为“快”迁移所有指标。
