# TOOL044｜Haystack

查询日期：2026-09-16。已核验事实仅限下列直接材料；实验未执行。

## 身份与直接证据

类型：检索与模型管道框架。

官方pipeline文档覆盖有向多重图、并行和循环，并区别取消异步执行与同步线程无法同样中止。

[EXT076｜Haystack 官方仓库](https://github.com/deepset-ai/haystack) [EXT077｜Haystack Pipelines](https://docs.haystack.deepset.ai/docs/pipelines)

## 可以吸收的具体成果

适合连接检索、过滤、重排、生成与评价，便于把一条管道拆成可测试组件。数据读写、权利和窗口由自有组件严格执行。

## 不适用与负面证据

框架循环、分支能力可能掩盖无界调用；UI取消不保证底层同步任务停止。通用RAG没有金融时点语义。

## 代码、数据与服务权利

核心仓库开源许可与托管平台、连接器、模型分别审阅；本輪未核全部可选插件。

## 运行与成本边界

Python组件和相应检索后端/模型。需要已有Python环境和清楚的持久状态策略，不为每个节点建服务。

## 维护证据及其限度

当前项目与详细pipeline文档可读，版本演进中；未执行旧新组件迁移和取消测试。

## 竞争替代与选型条件

简单直接模型调用加函数组合更便宜；当多种检索管道需反复实验时框架可降低连接成本。

## 最低成本核验实验

同一固定证据测试检索、引用和超时；取消后核对网络请求、worker和账本，不能只看页面停止动画。

## 两轴判断

**一般适用性：**检索管道平台值得对照。

**当前仓库适用性：**目前不默认替换Gemini适配；按复杂检索实验需求考虑。
