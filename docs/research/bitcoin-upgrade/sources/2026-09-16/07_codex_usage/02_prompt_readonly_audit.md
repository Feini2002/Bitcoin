# 可复制指令｜只读选择一次升级

请读取根README、当前F任务与终审，以及与我本次研究问题直接有关的专题/组件卡。再读取当前真实仓库，核对相关模块、数据流、设置、共享状态、缓存、导出、模型输入与报告历史。

本次先做只读对照和范围建议，不修改业务代码，不安装、接账号、调用付费模型、迁移数据库或部署。不要根据方案文件数自动执行任务；不要把旧事实报告当当前分支的代码证明。

请输出：
1. 这个问题当前怎样工作，附真实代码位置与状态边界。
2. 哪些方案已经实现，哪些确有差距；事实、推断、未知分别标记。
3. 至少一个可竞争替代，包括不增加新框架的方案。
4. 拟选外部成果的具体接口/源码/方法、许可与运行边界，不只写项目名。
5. 一个可独立交付切片：输入、输出、必要依赖、原WP子范围、影响文件、迁移、测试、失败回退。
6. 需要用户明确的费用/外部服务/生产边界；其余不受影响部分继续给完整设计。

特别检查收集增量：新方案是否得到新的独立证据，还是只把同一材料多总结一遍。来源健康不足时不能称无重大变化。方案中未启用的期权、ETF、L2和多代理不能被写为已参与确认。

最后给出推荐采用、先实验、仅借鉴、暂不采用四类结论。等待本次scope被确认后再执行，不把该文档视为全包执行授权。


当前范围与字段以10_final_review为准；旧包archive只供溯源，不要求重复实施旧路径。用户已经明确授权的scope不必逐个本地步骤再确认，但不能越出外部费用、生产和部署权限。
