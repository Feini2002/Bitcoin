---
description: 用户说明类回复默认轻量化，禁止未请求时的代码围栏块
alwaysApply: true
---

# 向用户说明（非「请给代码」场景）

- **先**用短句分点给出直接结论，再补前提或建议。
- **禁止**在聊天里使用 Markdown **多行代码围栏**（fenced code block）展示源码、伪代码、示例计算、长配置或多行脚本，**除非**用户在本轮对话中**明确**要求展示/张贴/用代码说明。
- 对比两套逻辑或溯源时，只用**文字**写差异与链路程式（算什么、数据是否同源、误差可能原因）；不要贴实现代码来论证。
- 少量行内反引号可标注**单个**文件名或**单行**命令；不要堆函数名列表代替正文。细则见仓库根目录 `.cursorrules` 中同名章节。
